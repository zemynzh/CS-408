# 程序查询方式 (co-07-02)

## 1. 基本原理
- CPU 不断循环查询 I/O 接口的状态位。
- **独占性**：在 I/O 操作期间，CPU 不能执行其他程序，效率低下。

## 2. 操作流程
1. CPU 发出读命令。
2. I/O 接口开始准备数据。
3. CPU 不断读取状态寄存器 (忙等)。
4. 接口准备好数据，CPU 从数据端口读取。

## 3. 适用场景
- 速度极慢的设备，或者嵌入式简单系统。
