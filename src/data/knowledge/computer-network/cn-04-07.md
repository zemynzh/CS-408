# IP 组播与路由器

## 1. IP 组播 (Multicast)
- **目的**：一对多传输，减少重复带宽。
- **地址**：D 类地址。
- **IGMP 协议**：主机与本地多播路由器之间的协议。
- **映射**：IP 多播地址映射到以太网多播 MAC 地址（01-00-5E 为前缀）。

## 2. 路由器
- **结构**：输入端口、交换结构、输出端口、路由选择处理器。
- **功能**：
  1. 路由选择（构建路由表）。
  2. 分组转发。
- **查表**：最长前缀匹配。
- **三层协议**：路由协议、IP 转发。
