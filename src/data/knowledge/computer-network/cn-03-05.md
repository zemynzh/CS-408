# 选择重传协议 (SR)

## 1. 基本原理
- 发送窗口 $W_T > 1$，接收窗口 $W_R > 1$。
- **独立确认**：每个帧单独确认。
- 乱序到达：接收方缓存乱序到达的帧，等缺失帧到达后再按序交付上层。

## 2. 窗口大小限制
对于 $n$ 位编号：
$$W_T + W_R \le 2^n$$
通常取 $W_T = W_R = 2^{n-1}$。
- 若窗口过大，会产生序号歧义。

## 3. 优点
- 减少不必要的重传，提高效率。
- 需要消耗更多的接收缓存。
