# 文件传输协议 (FTP)

## 1. 基本特点
- 提供异构环境下的文件传输。
- 底层：基于 **TCP**。

## 2. 两个连接
1. **控制连接 (Port 21)**：传输控制指令，整个会话期间保持打开。
2. **数据连接 (Port 20 或随机)**：传输实际文件。
   - **主动模式 (PORT)**：服务器主动连客户端。
   - **被动模式 (PASV)**：客户端连服务器。

## 3. 数据传输模式
- 文本模式 (ASCII)、二进制模式 (Binary)。
- 文件传输完成后，数据连接立即关闭。
