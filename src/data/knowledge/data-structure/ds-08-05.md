# 8.5 归并排序和基数排序

### 1. 归并排序 (Merge Sort)
将两个或多个有序表合并。
- **时间**：$O(n \log_2 n)$。
- **空间**：辅助数组 $O(n)$。
- **稳定性**：稳定。

### 2. 基数排序 (Radix Sort)
不基于比较。按位（个位、十位...）进行“分配”和“收集”。
- **时间**：$O(d(n+r))$。$d$ 为位数，$r$ 为基数。
- **稳定性**：稳定。
- **特点**：适用于 $n$ 很大但关键字范围小的情况。
