# 7.1 查找的基本概念

### 1. 基本术语
- **查找表**：同一类型的数据元素（或记录）组成的集合。
- **关键字 (Key)**：数据元素中某个项的值，用以标识该元素。
- **静态查找表**：只涉及查找、检索。
- **动态查找表**：查找的同时插入或删除元素。

### 2. 评价指标：平均查找长度 (ASL)
查找过程中进行关键字比较次数的平均值。
$$ ASL = \sum_{i=1}^n P_i C_i $$
- **$P_i$**：查找第 $i$ 个元素的概率。
- **$C_i$**：查找成功时找到第 $i$ 个元素所需进行的比较次数。
