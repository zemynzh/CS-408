# 8.1 排序的基本概念

### 1. 排序的定义
将一个数据元素序列按关键字递增（或递减）重新排列。

### 2. 稳定性
若 $k_i = k_j$，且排序前 $R_i$ 在 $R_j$ 前。若排序后 $R_i$ 仍在 $R_j$ 前，则算法是**稳定**的。

### 3. 分类
- **内部排序**：数据都在内存中。
- **外部排序**：数据量大，需借助磁盘。

> **考纲要求**：掌握算法原理、复杂度、稳定性和代码实现。
